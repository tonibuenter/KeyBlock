// (async () => {
//   const ipfs = await IPFS.create();
//   const { path } = await ipfs.add({
//     content: Buffer.from('Hello, IPFS!')
//   });
//
//   console.log('File added to IPFS:', path);
// })();

console.debug('ipfs-example nyi');
